# VitalFi Application Environment Variables

# Required: Solana vault authority public key
# This is the authority that manages vaults in the program
NEXT_PUBLIC_VAULT_AUTHORITY=

# Required: VitalFi Vault program ID
# The on-chain program address for vault operations
NEXT_PUBLIC_PROGRAM_ID=

# Backend API URL
# Defaults to http://localhost:3000 if not set
NEXT_PUBLIC_API_URL=http://localhost:3000

# Solana network to connect to
# Options: devnet, testnet, mainnet-beta
# Defaults to devnet if not set
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Optional: Custom Solana RPC endpoint
# If not provided, will use default public endpoint for the network
# Recommended for production to avoid rate limits
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=
